import type { NativeLayoutMetrics } from "../lib/reserved-regions";

export function NativeLayoutObserver(_props: {
  readonly onChange?: (metrics: NativeLayoutMetrics) => void;
}) {
  return null;
}
