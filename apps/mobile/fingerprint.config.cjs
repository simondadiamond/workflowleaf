// SDK 58 fingerprints native packages by version by default. Our pinned
// Screens PoC needs native source changes to invalidate the development client.
module.exports = { nativeModuleSourceType: "files" };
