Implement `.workflowleaf/plan.md`. It is the whole brief. Stay inside its
`## Change` list, and if the plan turns out to be wrong, change the plan file
first and say why in it.

Write the tests the plan names, run the repository's tests, and commit your
work on the current branch with a message that says what changed. Do not push.
Do not commit anything under `.workflowleaf/`.

If a correction arrives, it lists what failed and why. Fix the cause. A review
finding comes with a failure scenario: reproduce it with a test, then make the
test pass.
